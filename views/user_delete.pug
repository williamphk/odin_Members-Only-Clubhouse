extends layout 

block content 

    h1 #{title}: #{user_delete.fullname}

    //- if user_posts.length

    //-     p #[strong Delete the following posts before attempting to delete this user.]
        
    //-     div(style='margin-left:20px;margin-top:20px')
            
    //-     h4 Posts

    //-     each val in user_posts
    //-         hr
    //-         p #[strong Id:] 
    //-             a(href=val.url) #{val._id}
    //- else
    p Do you really want to delete this User?

    form(method='POST' action='')
        div.form-group
            input#userid.form-control(type='hidden',name='userid', required='true', value=user_delete._id )

        button.btn.btn-primary(type='submit') Delete